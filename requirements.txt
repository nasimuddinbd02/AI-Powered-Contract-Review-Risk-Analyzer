# ContractIQ backend dependencies
# Core API
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
python-multipart>=0.0.9
httpx>=0.27

# LLM + embeddings (optional at runtime; code falls back to mock mode if absent/no key)
anthropic>=0.39
openai>=1.30

# Ingestion / RAG (optional; pure-python fallbacks exist)
PyMuPDF>=1.24          # import name: fitz
pytesseract>=0.3.10    # OCR fallback (requires system tesseract)
numpy>=1.26
faiss-cpu>=1.8 ; python_version < "3.13"   # falls back to numpy store when unavailable
langchain-text-splitters>=0.2

# Auth (JWT). Password hashing uses stdlib PBKDF2; user store uses stdlib sqlite3.
PyJWT>=2.8

# Report export
reportlab>=4.1

# Testing
pytest>=8.1
