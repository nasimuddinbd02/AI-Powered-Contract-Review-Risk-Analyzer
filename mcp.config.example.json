{
  "//": "Connect an MCP host (e.g. Claude Desktop) to the ContractIQ MCP server.",
  "//start": "First run the API:  uvicorn contractiq.api.main:app --port 8000",
  "//note": "ContractIQ exposes a Streamable HTTP MCP server at /mcp (via fastapi-mcp). mcp-remote bridges stdio hosts to it.",
  "mcpServers": {
    "contractiq": {
      "command": "npx",
      "args": ["-y", "mcp-remote", "http://localhost:8000/mcp"]
    }
  }
}
